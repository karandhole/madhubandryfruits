/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',     // enables static export (next build + next export)
  // images: {
  //   unoptimized: true,  // disables Next.js image optimization
  // },
};

module.exports = nextConfig;
